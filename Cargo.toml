[package]
name = "microps-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
signal-hook = { version = "0.4.4", optional = true }

[features]
default = ["linux-userland"]
linux-userland = ["dep:signal-hook"]
